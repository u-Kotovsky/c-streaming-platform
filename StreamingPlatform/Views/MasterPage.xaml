<Page x:Class="StreamingPlatform.Views.MasterPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:control="clr-namespace:StreamingPlatform.UIElements"
      xmlns:helpers="clr-namespace:StreamingPlatform.Helpers"
      Title="Главная"
      Background="{StaticResource BackgroundBrush}"
      Loaded="Page_Loaded">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Навигационная панель -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="← Назад" Command="{Binding GoBackCommand}" Width="80"
                    Visibility="{Binding CanGoBack, Converter={StaticResource BoolToVis}}"/>
            <TextBlock Text="Стриминговая платформа" FontSize="22" FontWeight="Bold"
                       VerticalAlignment="Center" Margin="10,0"/>
        </StackPanel>

        <!-- Кнопки действий -->
        <WrapPanel Grid.Row="1" Margin="0,0,0,12">
            <Button Content="➕ Новый канал" Command="{Binding AddChannelCommand}"/>
            <Button Content="📺 Новая трансляция" Command="{Binding AddStreamCommand}"/>
        </WrapPanel>

        <!-- Каналы -->
        <TextBlock Grid.Row="2" Text="📢 Каналы" FontWeight="Bold" FontSize="16" Margin="0,0,0,4"/>
        <ScrollViewer Grid.Row="3" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" MaxHeight="200">
            <ItemsControl ItemsSource="{Binding StreamChannels}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <control:StreamChannelMiniature Title="{Binding Name}"
                                                        Description="{Binding Description}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Трансляции -->
        <TextBlock Grid.Row="4" Text="🎬 Трансляции" FontWeight="Bold" FontSize="16" Margin="0,16,0,4"/>
        <ScrollViewer Grid.Row="5" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding LiveStreams}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <control:LiveStreamMiniature Title="{Binding Title}"
                                                     Description="{Binding Status}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>