<UserControl x:Class="StreamingPlatform.UIElements.StreamChannelMiniature"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StreamingPlatform.UIElements"
             mc:Ignorable="d" 
             d:DesignHeight="24" d:DesignWidth="128">
    <UserControl.Resources>
        <Style x:Key="MovieCardStyle" TargetType="Border">
            <Setter Property="BorderBrush" Value="Black"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="CornerRadius" Value="4"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Border Style="{StaticResource MovieCardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <!--<RowDefinition Height="3*" />
                    <RowDefinition Height="auto"/>-->
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <!--<Rectangle Grid.Row="0" Width="128" Height="128" Fill="LightGray" VerticalAlignment="Top"/>-->
                    <!--<Image Grid.Row="0" Source="{Binding Path=Image}"/>-->
                <TextBlock Grid.Row="0" Text="{Binding Path=Title}" FontSize="12" Margin="4 0 4 0"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
